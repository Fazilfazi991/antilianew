export interface PlatformProfile {
  name: string;
  url?: string;
}

// Add the verified company profile URLs before publishing these links.
export const PLATFORM_PROFILES: PlatformProfile[] = [
  { name: 'Property Finder Qatar' },
  { name: 'Qatar Living' },
  { name: 'Mzad Qatar' },
];
